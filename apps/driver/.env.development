# Local development API URL
# Using Windows host IP (accessible from phone on same Wi-Fi)
EXPO_PUBLIC_API_URL=http://192.168.101.7:3000/api

# Tell Expo Metro bundler to use Windows IP (for WSL → phone connectivity)
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.101.7
