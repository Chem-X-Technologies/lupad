{
  "name": "lupad",
  "version": "0.1.0",
  "private": true,
  "description": "Lupad - Ride-hailing app for Calbayog City",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "eas-cli": "^16.28.0",
    "prettier": "^3.2.4",
    "turbo": "^2.3.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=22.0.0"
  }
}